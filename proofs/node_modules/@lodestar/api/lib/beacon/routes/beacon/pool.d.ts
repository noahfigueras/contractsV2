import { phase0, altair, capella, CommitteeIndex, Slot } from "@lodestar/types";
import { ApiClientResponse } from "../../../interfaces.js";
import { HttpStatusCode } from "../../../utils/client/httpStatusCode.js";
import { RoutesData, ReturnTypes, ReqSerializers, ReqEmpty } from "../../../utils/index.js";
export type AttestationFilters = {
    slot: Slot;
    committeeIndex: CommitteeIndex;
};
export type Api = {
    /**
     * Get Attestations from operations pool
     * Retrieves attestations known by the node but not necessarily incorporated into any block
     * @param slot
     * @param committeeIndex
     * @returns any Successful response
     * @throws ApiError
     */
    getPoolAttestations(filters?: Partial<AttestationFilters>): Promise<ApiClientResponse<{
        [HttpStatusCode.OK]: {
            data: phase0.Attestation[];
        };
    }, HttpStatusCode.BAD_REQUEST>>;
    /**
     * Get AttesterSlashings from operations pool
     * Retrieves attester slashings known by the node but not necessarily incorporated into any block
     * @returns any Successful response
     * @throws ApiError
     */
    getPoolAttesterSlashings(): Promise<ApiClientResponse<{
        [HttpStatusCode.OK]: {
            data: phase0.AttesterSlashing[];
        };
    }>>;
    /**
     * Get ProposerSlashings from operations pool
     * Retrieves proposer slashings known by the node but not necessarily incorporated into any block
     * @returns any Successful response
     * @throws ApiError
     */
    getPoolProposerSlashings(): Promise<ApiClientResponse<{
        [HttpStatusCode.OK]: {
            data: phase0.ProposerSlashing[];
        };
    }>>;
    /**
     * Get SignedVoluntaryExit from operations pool
     * Retrieves voluntary exits known by the node but not necessarily incorporated into any block
     * @returns any Successful response
     * @throws ApiError
     */
    getPoolVoluntaryExits(): Promise<ApiClientResponse<{
        [HttpStatusCode.OK]: {
            data: phase0.SignedVoluntaryExit[];
        };
    }>>;
    /**
     * Get SignedBLSToExecutionChange from operations pool
     * Retrieves BLSToExecutionChange known by the node but not necessarily incorporated into any block
     * @returns any Successful response
     * @throws ApiError
     */
    getPoolBlsToExecutionChanges(): Promise<ApiClientResponse<{
        [HttpStatusCode.OK]: {
            data: capella.SignedBLSToExecutionChange[];
        };
    }>>;
    /**
     * Submit Attestation objects to node
     * Submits Attestation objects to the node.  Each attestation in the request body is processed individually.
     *
     * If an attestation is validated successfully the node MUST publish that attestation on the appropriate subnet.
     *
     * If one or more attestations fail validation the node MUST return a 400 error with details of which attestations have failed, and why.
     *
     * @param requestBody
     * @returns any Attestations are stored in pool and broadcast on appropriate subnet
     * @throws ApiError
     */
    submitPoolAttestations(attestations: phase0.Attestation[]): Promise<ApiClientResponse<{
        [HttpStatusCode.OK]: void;
    }, HttpStatusCode.BAD_REQUEST>>;
    /**
     * Submit AttesterSlashing object to node's pool
     * Submits AttesterSlashing object to node's pool and if passes validation node MUST broadcast it to network.
     * @param requestBody
     * @returns any Success
     * @throws ApiError
     */
    submitPoolAttesterSlashings(slashing: phase0.AttesterSlashing): Promise<ApiClientResponse<{
        [HttpStatusCode.OK]: void;
    }, HttpStatusCode.BAD_REQUEST>>;
    /**
     * Submit ProposerSlashing object to node's pool
     * Submits ProposerSlashing object to node's pool and if passes validation  node MUST broadcast it to network.
     * @param requestBody
     * @returns any Success
     * @throws ApiError
     */
    submitPoolProposerSlashings(slashing: phase0.ProposerSlashing): Promise<ApiClientResponse<{
        [HttpStatusCode.OK]: void;
    }, HttpStatusCode.BAD_REQUEST>>;
    /**
     * Submit SignedVoluntaryExit object to node's pool
     * Submits SignedVoluntaryExit object to node's pool and if passes validation node MUST broadcast it to network.
     * @param requestBody
     * @returns any Voluntary exit is stored in node and broadcasted to network
     * @throws ApiError
     */
    submitPoolVoluntaryExit(exit: phase0.SignedVoluntaryExit): Promise<ApiClientResponse<{
        [HttpStatusCode.OK]: void;
    }, HttpStatusCode.BAD_REQUEST>>;
    /**
     * Submit SignedBLSToExecutionChange object to node's pool
     * Submits SignedBLSToExecutionChange object to node's pool and if passes validation node MUST broadcast it to network.
     * @param requestBody
     * @returns any BLSToExecutionChange is stored in node and broadcasted to network
     * @throws ApiError
     */
    submitPoolBlsToExecutionChange(blsToExecutionChange: capella.SignedBLSToExecutionChange[]): Promise<ApiClientResponse<{
        [HttpStatusCode.OK]: void;
    }, HttpStatusCode.BAD_REQUEST>>;
    /**
     * TODO: Add description
     */
    submitPoolSyncCommitteeSignatures(signatures: altair.SyncCommitteeMessage[]): Promise<ApiClientResponse<{
        [HttpStatusCode.OK]: void;
    }, HttpStatusCode.BAD_REQUEST>>;
};
/**
 * Define javascript values for each route
 */
export declare const routesData: RoutesData<Api>;
export type ReqTypes = {
    getPoolAttestations: {
        query: {
            slot?: number;
            committee_index?: number;
        };
    };
    getPoolAttesterSlashings: ReqEmpty;
    getPoolProposerSlashings: ReqEmpty;
    getPoolVoluntaryExits: ReqEmpty;
    getPoolBlsToExecutionChanges: ReqEmpty;
    submitPoolAttestations: {
        body: unknown;
    };
    submitPoolAttesterSlashings: {
        body: unknown;
    };
    submitPoolProposerSlashings: {
        body: unknown;
    };
    submitPoolVoluntaryExit: {
        body: unknown;
    };
    submitPoolBlsToExecutionChange: {
        body: unknown;
    };
    submitPoolSyncCommitteeSignatures: {
        body: unknown;
    };
};
export declare function getReqSerializers(): ReqSerializers<Api, ReqTypes>;
export declare function getReturnTypes(): ReturnTypes<Api>;
//# sourceMappingURL=pool.d.ts.map