import { getGenericJsonServer, registerRoute, } from "../../utils/server/index.js";
import { routesData, getReturnTypes, getReqSerializers } from "../routes.js";
export function getRoutes(config, api) {
    // All routes return JSON, use a server auto-generator
    return getGenericJsonServer({ routesData, getReturnTypes, getReqSerializers }, config, api);
}
export function registerRoutes(server, config, api) {
    const routes = getRoutes(config, api);
    for (const route of Object.values(routes)) {
        registerRoute(server, route);
    }
}
//# sourceMappingURL=index.js.map